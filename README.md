# Follow Up Application 

L'application permet la suivie des leads et followup des differents utilisateurs à savoir le super admin et les sub admin. 

## Supabase

1. Copy `.env.example` to `.env`.
2. Set `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` with your Supabase project values.

